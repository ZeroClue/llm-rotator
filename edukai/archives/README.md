# Archives

Graduated domains live here, folder intact and untouched. Nothing in here is deleted — a domain is parked because it stopped mattering, not because it was wrong.

To graduate a domain: move its folder from `curriculum/` into this directory and set its Syllabus row to `graduated`. If it becomes relevant again, move it back and set `active`.
