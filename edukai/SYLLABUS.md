# Syllabus

Master index of everything this store has been taught. Read this first, then descend into the domain you need. Agents maintain this file whenever domains change — see `AGENTS.md`.

This file is on the cache hot path: rows are append-only and reordering is forbidden. Re-verification dates live in each domain's `review.md`, not here.

Statuses: `active` · `reference` · `graduated` (folder moved to `archives/`)

## Domains

| Domain | Status | Notes |
|---|---|---|
| [codebase-llm-rotator](curriculum/codebase-llm-rotator/) | active | This repo's runtime wiring: lazy app build, injection seams, structural traps |

<!-- Register new domains here, appending at the bottom:
| [name](curriculum/name/) | active | what it covers |
-->
