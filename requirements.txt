# Runtime dependencies for the LLM rotator proxy.
#
# Pinned for reproducible builds (this file feeds the Docker image).
# Bump deliberately: re-run `python -m pytest tests/` after any change.
#
# Optional extras NOT included here:
#   llmlingua  - semantic compression stage; pulls in torch (~2GB).
#                Enable with ENABLE_SEMANTIC_COMPRESSION=true after installing it.
flask==3.1.3
gunicorn==26.1.0
requests==2.34.2
tiktoken==0.14.0
